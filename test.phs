data Nat [
  Zero: Nat;
  Suc: Nat => Nat;
];

paraFunc: a => a = [
  x: a -> x;
];

log: String => {Unit} = str : String -> `() => console.log($str)`: {Unit};

main : {Unit} = do{
  _ : Unit <- log("Hello Phosphor!");
  return Unit;
};
